---
name: 剧本跟拍生图策略对齐
overview: 将 剧本-跟拍.ini 的生图策略伪代码（第45-64行）替换为与对口型-跟拍一致的简化版本，去掉 `group_photo`、`ID2i`、分镜分配逻辑。
todos:
  - id: replace-strategy
    content: 替换生图策略伪代码（第45-64行）为对口型-跟拍的简化版本
    status: pending
isProject: false
---

# 剧本-跟拍.ini 生图策略对齐

文件：[/Users/bytedance/Downloads/## 剧本-跟拍.ini](/Users/bytedance/Downloads/## 剧本-跟拍.ini)

## 修改内容

将第45-64行的生图策略伪代码替换为对口型-跟拍的简化版本。保留 `（仅核心主旨=剧本时）` 的限定说明。

**去掉的内容：**

- `group_photo` 分支（多主体 else、分镜不匹配 fallback）
- `ID2i` 引用
- `图片数 != 分镜数` 的判断逻辑
- `for each 分镜` 的循环逻辑

**替换为：**

```
if 用户明确说"不生图/直接用我的图":
    所有分镜用 image_mapping

elif 单张图片是否多主体 == true and 无编辑需求:
    用 image_mapping（所有图都送给视频工具）

else:
    参考生图工具定义，选择合适的生图工具
```

仅改生图策略伪代码部分，其他内容不动。