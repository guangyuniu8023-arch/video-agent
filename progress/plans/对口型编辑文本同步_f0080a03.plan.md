---
name: 对口型编辑文本同步
overview: 对 `## 对口型` 文件应用与 `## 动作.ini` 相同的修改：当生图工具的 prompt 有内容时，同样的内容也要追加拼接到 video_prompt 中，原有 video_prompt 逻辑不变。
todos:
  - id: update-rule-kouqing
    content: 更新第26-36行的编辑文本处理规则，补充生图 prompt 追加到 video_prompt 的拼接逻辑
    status: completed
  - id: update-example2-kouqing
    content: 更新示例2：video_prompt 追加 ID2i 的 prompt 内容（把衣服变成红色）
    status: completed
  - id: update-example5-kouqing
    content: 更新示例5：video_prompt 追加 AI_create 的 prompt 内容（多图融合指令）
    status: completed
isProject: false
---

# 对口型文件 - 编辑文本同步到 video_prompt

## 修改文件

[/Users/bytedance/Downloads/## 对口型](/Users/bytedance/Downloads/## 对口型)

## 修改内容

### 1. 更新"编辑文本处理"规则（第26-36行）

当前逻辑已经做了图像编辑/视频动态拆分，但缺少拼接规则：

```
else:
    分析用户文本，拆分为两类需求：
    - 图像编辑需求 → 填入对应生图工具的 prompt
    - 视频动态需求 → 填入 video_prompt
```

需要补充：当生图工具 prompt 非空时，其内容也要追加到 video_prompt。最终 `video_prompt = 视频动态需求 + 生图编辑需求（拼接）`。

### 2. 更新示例2（第247-270行）- 纯图像编辑需求

当前：ID2i `prompt = "把衣服变成红色"`，`video_prompt = ""`

- ID2i prompt 非空 → 追加到 video_prompt
- 修改后：`video_prompt = "把衣服变成红色"`

### 3. 更新示例5（第330-353行）- AI_create 多图融合

当前：AI_create `prompt = "让{user_image1}穿{user_image2}的衣服，在{user_image3}的场景里，旁边站着{user_image4}的人"`，`video_prompt = ""`

- AI_create prompt 非空 → 追加到 video_prompt
- 修改后：`video_prompt = "让{user_image1}穿{user_image2}的衣服，在{user_image3}的场景里，旁边站着{user_image4}的人"`

### 4. 无需修改的示例

- 示例1：拍同款，prompt 为空 → 无需追加
- 示例3：纯视频动态，ID2i prompt 为空 → 无需追加
- 示例4：保留视频元素，prompt 为空 → 无需追加
- 示例6/7：image_mapping，无 prompt → 无需追加
- 示例8：视频动态需求，ID2i prompt 为空 → 无需追加
- 示例9/10/11：prompt 为空 → 无需追加

