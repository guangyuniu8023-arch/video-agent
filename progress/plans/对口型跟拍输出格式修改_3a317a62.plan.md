---
name: 对口型跟拍输出格式修改
overview: 将 `对口型-跟拍` 文件中 `image_generate_rule` 的输出格式从字典结构改为扁平数组结构，与之前 `角色定义.ini` 的修改保持一致。
todos:
  - id: format-def
    content: 修改输出格式定义（第91-96行），image_generate_rule 从字典改为扁平数组
    status: completed
  - id: examples-1-11
    content: 修改全部 11 个 Few-shot 示例的输出格式，去掉主体类型包裹层
    status: completed
isProject: false
---

# 对口型-跟拍 输出格式修改

文件：[/Users/bytedance/Downloads/## 对口型-跟拍](/Users/bytedance/Downloads/## 对口型-跟拍)

## 修改点1：输出格式定义（第91-96行）

**当前：**

```json
"image_generate_rule": {
  "图片": [...],
  "IP": [...],
  "ID": [...]
}
```

**改为：**

```json
"image_generate_rule": [...]  // 按分镜顺序，直接包含 tool 对象
```

## 修改点2：全部 11 个 Few-shot 示例

去掉每个示例输出中 `"图片"`/`"IP"`/`"ID"` 的包裹层，tool 对象直接放入扁平数组：

- **示例1**（第273行）：`"图片": [{album_CY...}]` -> `[{album_CY...}]`
- **示例2**（第306行）：`"图片": [{album_CY...}]` -> `[{album_CY...}]`
- **示例3**（第339行）：`"图片": [{image_mapping...}]` -> `[{image_mapping...}]`
- **示例4**（第370行）：`"图片": [{...}, {...}, {...}]` -> `[{...}, {...}, {...}]`
- **示例5**（第405行）：`"图片": [{AI_create...}]` -> `[{AI_create...}]`
- **示例6**（第436行）：`"IP": [{IP_CY...}]` -> `[{IP_CY...}]`
- **示例7**（第467行）：`"ID": [{image_mapping...}]` -> `[{image_mapping...}]`
- **示例8**（第498行）：`"图片": [{...}], "IP": [{...}]` -> `[{...}, {...}]`（混合主体合并为一个扁平数组）
- **示例9**（第532行）：`"图片": [{album_CY...}]` -> `[{album_CY...}]`
- **示例10**（第565行）：`"图片": [{album_CY...}]` -> `[{album_CY...}]`
- **示例11**（第598行）：`"图片": [{album_CY...}]` -> `[{album_CY...}]`

## 变更原则

- 只修改输出格式结构，不改 tool 名称、策略逻辑、输入格式
- 总计 12 处修改（1处格式定义 + 11个示例）

